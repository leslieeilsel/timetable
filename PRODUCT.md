# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 教务管理员和排课员在桌面端维护学校基础资料、任课关系、排课版本以及长期和临时调课。
- 教师通过独立部署的移动端 H5 登录，只查看与本人有关的实际课表，不进入教务工作台或其他业务功能。
- 全校只读查看者与教师是不同角色；查看者可以查看学校课表，教师只能查看本人课表。

## Product Purpose

系统用于维护学校学年、学期、班级、教师、课程、教室和任课关系，生成并调整课表，并把长期变更、日期例外和代课统一解析为指定日期的实际课表。成功意味着教务能够安全调整安排，教师始终看到本人当日和本周真实应执行的课程。

## Operating Context

- 教务端位于现有 `apps/web`，以桌面排课和日常运行管理为主。
- 教师端位于独立的 `apps/teacher-web`，独立构建并部署到二级域名。
- 两个前端共享同一套基础视觉组件和设计令牌，但路由、页面、权限和构建产物独立。
- 后端为 Laravel API，使用本地账号、Sanctum Session Cookie 和学校时区解析日期。

## Capabilities and Constraints

- 教师账号必须与唯一教师资料绑定，教师 ID 从登录身份推导，客户端不能指定其他教师。
- 教师端只提供登录、修改临时密码、退出和本人课表查看。
- 教师课表以实际日期为准，包含生效中的长期课表版本、临时调课、停课和代课结果。
- 临时调课只影响明确日期；长期调课通过有生效日期范围的不可变课表版本表达。
- 同一学期的课表生效区间不能重叠，发布长期版本时必须重新验证范围内已有的日期例外和代课。
- 手机端采用按日期和时间纵向阅读的布局，不把桌面课表压缩成横向大表格。

## Brand Commitments

- 延续现有教务系统的中文术语、标志、石墨中性色和状态色。
- 视觉克制、清晰、可信；颜色只服务于课程区分和调课状态。

## Evidence on Hand

- 现有教务端组件、令牌和品牌资产位于 `apps/web/src` 与 `apps/web/public/brand`。
- 产品与架构资料位于 `docs/requirements` 和 `docs/architecture`。
- 当前实现已经包含基础周课表、课表版本、日期例外、请假和代课能力。

## Product Principles

- 权限由后端身份边界保证，不能依赖前端隐藏。
- 任何日期的实际课表都可解释、可追踪，并且不会被之后发布的版本反向改写。
- 长期调整改变一个日期区间的基础安排，临时调整只覆盖明确日期。
- 教师端只呈现当天需要采取行动的信息，避免暴露教务复杂度。

## Accessibility & Inclusion

- 核心流程目标符合 WCAG 2.2 AA。
- 移动端触控目标、文字尺寸、状态文案和焦点状态必须可辨识，调整状态不能只依赖颜色表达。
